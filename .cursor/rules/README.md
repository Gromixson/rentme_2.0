# Cursor rules — RentMe 2.0

| Plik | Kiedy się stosuje | Źródło |
|------|-------------------|--------|
| `rentme-project.mdc` | Kod aplikacji (`src/`, Angular, Firebase) | Projekt (ręcznie) |
| `10x-course.mdc` | `context/`, łańcuch 10x, AGENTS.md, lekcje kursu | `10x-cli` (aktualizuj między `BEGIN`/`END`) + sekcja RentMe po `END` |

**Nie** edytuj treści między `<!-- BEGIN @przeprogramowani/10x-cli -->` a `END` — nadpisze to `10x get m1lN`.

Po `10x get` sprawdź, czy sekcja **RentMe 2.0 — project overrides** pod `END` nadal jest na miejscu.
